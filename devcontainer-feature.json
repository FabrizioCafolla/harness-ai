{
  "id": "harness-ai",
  "version": "0.1.1",
  "name": "Harness AI",
  "description": "Scaffolds AI agent and skill assets (Claude Code, OpenCode) into the workspace. All configuration — tools, install toggles, content repo — lives in the workspace's .harness-ai/config.yaml, the single source of truth; this feature has no options. Provisions at container create via a config-driven CLI (harnessai) and re-syncs on every start when content has changed.",
  "documentationURL": "https://containers.dev/features",
  "installsAfter": ["ghcr.io/devcontainers/features/python:1"],
  "postCreateCommand": "harnessai install",
  "postStartCommand": "harnessai sync"
}
